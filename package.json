{
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "@j178/prek": "^0.2.29"
  },
  "scripts": {
    "prepare": "[ -n \"$CI\" ] || prek install",
    "postinstall": "prek install"
  }
}
